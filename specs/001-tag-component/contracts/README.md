# Contracts for Shareable Tag Component

This feature does not introduce new cross-process or external contracts beyond
existing webview messaging. All behavior is contained within the extension's
React components. No additional contract files are required.