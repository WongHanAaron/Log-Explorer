# Contracts

This feature does not introduce any new public interfaces or external contracts beyond
existing message schemas used by the webview. All communication occurs via internal
`HostToWebviewMessage`/`WebviewToHostMessage` types already defined elsewhere.