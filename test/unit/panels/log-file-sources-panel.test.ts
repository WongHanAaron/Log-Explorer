import * as assert from 'assert';

describe('LogFileSourcesPanel', () => {
    it('skipped - behaviour is validated in the webview integration tests instead', function () {
        this.skip();
    });
});
